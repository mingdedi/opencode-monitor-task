---
name: Bug report
about: Report something that is not working as expected
labels: bug
---

**Environment**

- OpenCode version (`opencode --version`):
- Node version (`node -v`):
- OS:
- Plugin install method: [ `opencode plugin add github:mingdedi/opencode-monitor-task` | npm | `install-local.sh` ]

**What happened**

<!-- What you did, what you expected, what actually happened. -->

**Minimal reproduction**

```sh
# The monitor call or monitored command that triggers the issue
```

**Logs**

Server log (`$TMPDIR/opencode/opencode-monitor-task.log`):

```
# paste relevant excerpts
```

TUI log (`$TMPDIR/opencode-monitor-tui.log`), if the sidebar panel is affected:

```
# paste relevant excerpts
```

<!-- Redact any secrets before posting. -->
